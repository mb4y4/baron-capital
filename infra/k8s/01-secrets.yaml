# Do NOT commit real secrets. Use `kubectl create secret generic ...` directly,
# or a secrets manager (AWS Secrets Manager / Azure Key Vault / GCP Secret Manager)
# integrated via CSI driver, instead of checking this file in with real values.
apiVersion: v1
kind: Secret
metadata:
  name: baron-secrets
  namespace: baron-capital
type: Opaque
stringData:
  DJANGO_SECRET_KEY: "REPLACE_ME"
  POSTGRES_DB: "baron_capital"
  POSTGRES_USER: "baron"
  POSTGRES_PASSWORD: "REPLACE_ME"
